## 2026-07-02

Branch: `work/base-9caa062`

What we changed in this branch:

- Fixed reports so export/loading works again.
- Hid batch stop controls for director/guest accounts and kept them for admin only.
- Added telemetry settings for motion filtering:
  - movement speed threshold
  - movement confirm packets
  - loader max distance from host
  - loader offline timeout
- Reworked motion filtering so weight jumps while moving do not:
  - start a batch
  - add ingredients
  - distort loading segments
- Made exit from moving state immediate on zero speed.
- Ignored stale/far loader RTK points:
  - no future RTK point usage
  - no loader substitution when loader is too far from host
  - no loader substitution when loader is offline
- Fixed batch start detection to use the real weight baseline instead of late segment close time.
- Replayed raw telemetry through the updated algorithm with `server/scripts/replay-batches-from-telemetry.mjs`.
- Fixed order validation logic:
  - order is checked relatively
  - skipped components are reported as missing instead of order mismatch
- Fixed batch group reassignment during unload/autoclose:
  - final drive-through after unload no longer rewrites the batch group
  - batch `#5` on `2026-07-01` now stays on `Дойное (4)` instead of switching to `Случка/предслучка`

Known context from investigation:

- Batch `#4` on `2026-07-01` had a late-start bug; start now replays from the earlier real baseline.
- Batch `#5` was reassigned by late packets after unload; this is fixed.
- Batch `#3` shows large `СилоС`, but current replay indicates the weight was added while the host was still in `СилоС` zone, not from an out-of-zone phantom spike.

Files added for support/testing:

- `server/scripts/motion-filter-test.mjs`
- `server/scripts/replay-batches-from-telemetry.mjs`
- `server/prisma/migrations/20260702000100_add_loader_effective_position_settings/`
